# `leap` is the TLS protocol every current bridge speaks, and needs a paired certificate.
#
# There was a second package here, `integration`, driving the telnet integration port. Only the
# Smart Bridge PRO ever exposed that port — on a standard bridge nothing was listening — so it
# was a whole control path most houses could not use. Dropped rather than carried.
[workspace]
resolver = "3"
members = ["leap"]

# A driver is a dylib the controller loads; a debug-sized one is several times larger to ship.
[profile.release]
opt-level = 2
codegen-units = 16
